<p id="notice"><%= notice %></p>
<%= link_to 'Microposts', new_micropost_path %>
<h1>Users</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <body>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= link_to 'Show', user %></td>
        <td><%= link_to 'Edit', edit_user_path(user) %></td>
      </tr>
    <% end %>
  </body>
</table>

<br>

<%= link_to 'New User', new_user_path %> |
<%= link_to 'Back', microposts_path %>
